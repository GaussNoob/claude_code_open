import { createUnavailableTool } from '../createUnavailableTool.js'

export const SuggestBackgroundPRTool = createUnavailableTool(
  'SuggestBackgroundPR',
  'Background PR suggestions are unavailable in this reconstructed build.',
)
