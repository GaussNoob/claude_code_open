/** @typedef {object} ClaudeForChromeContext Stub — real package is internal. */

export const BROWSER_TOOLS = []

export function createClaudeForChromeMcpServer(_ctx) {
  return {
    async connect(_transport) {},
  }
}
